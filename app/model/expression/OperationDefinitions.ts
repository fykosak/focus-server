import OperationManager from "@app/model/expression/OperationManager";

/**
 * Function to add new operations and their overloads
 */
type OperationDefinitions = (operationManager: OperationManager) => void;

export default OperationDefinitions;
